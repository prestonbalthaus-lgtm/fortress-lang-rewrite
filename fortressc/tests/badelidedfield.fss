(* AN OBJECT'S VALUE PARAMETERS ARE ITS FIELDS, and a field needs a name to be
   read by. `List[\ZZ32\]` cannot be one, so this is the elision case; a bare
   identifier is the untyped-FIELD case and is in `baduntypedfield.fss`. *)

object O(List[\ZZ32\]) end

run(): () = ()
