component divoverflow32

export Executable

quotient(a: ZZ32, b: ZZ32): ZZ32 = a/b

run():()=do
   least: ZZ32 := -2147483647
   least := least - 1
   println("stdout survived the halt")
   println(least)
   println(quotient(least, -1))
   println("unreachable")
end
end
